import { State, AsyncState } from "./state";
import { Ref, RefState } from "./ref";
import {
  CookieState,
  SessionStorageState,
  LocaleStorageState,
} from "./storage";
import { ApiState, ApiStoredState } from "./api";

export const eUse = {
  State,
  AsyncState,
  Ref,
  RefState,
  CookieState,
  SessionStorageState,
  LocaleStorageState,
  ApiState,
  ApiStoredState,
};
