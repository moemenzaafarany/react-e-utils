import { State, AsyncState } from "./state";
import { Ref } from "./ref";
import {
  CookieState,
  SessionStorageState,
  LocaleStorageState,
} from "./storage";
import { ApiState, ApiStoredState } from "./api";

export const eUse = {
  State,
  AsyncState,
  Ref,
  CookieState,
  SessionStorageState,
  LocaleStorageState,
  ApiState,
  ApiStoredState,
};
